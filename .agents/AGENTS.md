# Project Specific Rules

- **Database:** This project uses a real PostgreSQL database with an Express backend (`server.js`). Do NOT assume data is mocked or only in localStorage. Always check `server.js` for real backend API routes and table schemas when adding features that require data persistence.
